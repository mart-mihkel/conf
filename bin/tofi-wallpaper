#!/usr/bin/env bash

WALLPAPERS=~/Pictures/walls
PICK=$(ls $WALLPAPERS | tofi)
[[ -z $PICK ]] && exit 1

awww img $WALLPAPERS/$PICK \
    --transition-type random \
    --transition-duration 2 \
    --transition-fps 60
